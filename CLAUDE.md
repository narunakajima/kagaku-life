# くらしを変える科学 — プロジェクト憲法

## このプロジェクトについて

**くらしを変える科学（kagaku-life）** はAI・ロボティクス分野の学術論文を一般視聴者向けに解説する
日本語YouTubeチャンネルの制作リポジトリ。詳細な企画は `~/Desktop/企画書_くらしを変える科学.pdf` を参照。

**対外的なチャンネル名は「幸せな未来のサイエンスチャンネル」に確定した
（2026-08-21）。** 「くらしを変える科学」「kagaku-life」はリポジトリ・
エピソードID（`kl{NNN}`）等で使う社内コードネームとして引き続き使用する。
YouTubeの動画タイトル・概要欄・タグ・チャンネル名など視聴者が目にする
対外的な表記はすべて「幸せな未来のサイエンスチャンネル」を使うこと。

> ディレクトリ構成・制作パイプライン（スクリプト・ワークフロー）は今後段階的に整備する。
> 論文選定（STAGE1〜5）は `/kl-topic` で実行できる（`.claude/commands/kl-topic.md`）。
> エピソードJSON生成以降の制作パイプラインは未着手。

---

## ストレージ方針（2026-08-21確定）

samurai-chronicles・ランプのひとりごとと同じ運用とする。

- **Desktop（`~/Desktop/kagaku-life/{episode}/`）: 確認用。** `kl_image_gen.py`・
  `kl_tts_gen.py` の生成物はまずここに保存し、内容を確認・採用してから次工程へ進む。
- **Google Drive（`Kagaku-Life/KL{NNN}/`）: コンテンツ格納用。** 確認・採用済みの
  素材を `kl_finalize.py --episode kl{NNN}` でGoogle Driveのローカル同期フォルダへ
  コピーする（Google DriveのフォルダIDは `Kagaku-Life` フォルダ配下、エピソード
  フォルダ名は大文字化した `KL{NNN}`。SC/LWの命名規則に合わせる）。

**既知の制約:** この環境ではmacOSの権限制約により、ターミナルからの
`ls`／ディレクトリ一覧（`iterdir()`等）がDesktop・Google Driveのローカル同期
フォルダに対して失敗することがある（既知のTCC権限問題、過去に再起動で解消した
実績あり）。一方、既知のファイル名を指定した直接の読み書き（`open()`/`Path.write_bytes()`
等）は問題なく動作する。そのため `kl_finalize.py` はディレクトリ一覧を使わず、
`episodes/kl{NNN}.json` の `scene_id` から期待されるファイル名を組み立てて
1件ずつ処理する設計にしている。

---

## チャンネルロゴ（2026-08-21確定）

samurai-chroniclesの実際のロゴ（`Kagaku-Life/../samurai-chronicles/LOGO.PNG`）を
参考に、同じ「円形バッジ／エンブレム」形式で制作した。保存先: Google Drive
`Kagaku-Life/LOGO.PNG`（1280×1280px）。

**デザイン:** 中央に暖色の光る核を持つ原子（オービット）のアイコン——
「サイエンスが暮らしに温かさ・幸せをもたらす」というチャンネルコンセプトを
象徴する。二重リングの縁取り、テクスチャのある紺〜チールの背景、金の
アクセントラインという、SCと共通する「プレミアムなエンブレム」の質感を踏襲。
配色はSCの臙脂×金ではなく、番組本編の配色（深い青・チール＋暖色コーラルの
アクセント）に合わせた。

**制作方法（今後の再生成・別チャンネルへの応用のため記録）:**
1. `gemini-3.1-flash-image` でアトムのアイコンのみを単体生成（無地の紺背景、
   円のリングや文字は含めない）
2. 背景色との色距離でアルファチャンネルを作り、アイコン部分だけを透過切り出し
   （単純なしきい値だと背景のわずかなグラデーションノイズまで拾ってしまうため、
   ノイズを無視できるだけの距離しきい値を取る必要があった）
3. リング・区切り線・文字はPillowで手動描画。**チャンネル名のような正確さが
   必要なテキストはAI画像生成に任せず、実フォント（ヒラギノ角ゴシック W8/W9）
   でPillow合成する**（日本語は特に誤字・崩れのリスクが高いため）
4. アイコンサイズとテキストサイズ・上下余白は、両方を大きくしようとすると
   衝突するため、円の半径から逆算して余白が均等になるよう調整した

---

## マシン判定ルール

MacBook・iMac の両方から運用する（`samurai-chronicles` / `lamp-whisper` と同様）。
git操作（pull / push / status など）の前に必ず `hostname` でマシンを確認する。

| hostname | マシン |
|---|---|
| `naru-iMac.local` | iMac |
| それ以外（例: `naru-MacBook-Air.local`） | MacBook（または別端末） |

ユーザーが「このマシンは〇〇です」と教えてくれた場合はその情報を優先する。

---

## 論文選定ロジック（目利きプロセス）

企画書§05「目利きプロセスの役割分担」を、実際に運用できる基準まで具体化したもの。
エピソードのテーマ（＝扱う論文）を決める際は必ずこの5段階を経由する。

### STAGE 1 — 一次収集（Semantic Scholar Graph APIによる自動検索）

**収集源: Semantic Scholar Graph APIのみ。**（2026-08確定）CS・工学・医学・経済学を横断する
最大級の学術DBで、PubMed/MEDLINEのコーパスも取り込んでいるため医療系カテゴリもある程度カバーできる。
1つのAPIに絞ることで認証・レート制限管理をシンプルに保つ。運用してみて特定カテゴリ（特に③医療補助
技術）の結果が薄いと判明した場合のみ、PubMed E-utilities等の追加を検討する。

**arXiv等のプレプリントもSTAGE1の候補源に含める。**（2026-08改訂）AI・ロボティクス分野は
動きが速く、査読を待っている間に現実の技術が追い越してしまう。企画書の核心は
「論文が10年後の暮らしを変える」＝信頼できる**先取り**であり、「安全に検証済みだが
地味な話」を選ぶことではない。査読完了を必須条件にすると、この分野で最も
「暮らしを劇的に変える可能性がある」トピックを機械的に取りこぼすリスクの方が大きいと
判断し、査読済みのみへの限定は撤回した（旧ルール・撤回の経緯はSTAGE2参照）。

**なぜWeb検索（`WebSearch`ツール）ではダメか:** 汎用のWeb検索は1クエリあたり上位10件程度を
SEO・話題性ベースで返すだけで、世界の論文母集団（arXivだけで年間数十万本規模）に対して
実質的な検索にならない。Semantic ScholarのようなAPIを直接叩き、**実際の総ヒット件数を
必ずログに残す**（「過去3年・該当条件で件数◯件」を毎回記録し、見えている数本を無言で
母集団として扱わない）。

**サブジャンルカテゴリとクエリ語彙:** `query_vocabulary.json` に6カテゴリ分のクエリ候補を
保存している（`kl_paper_search.py` が読み込む想定）。カテゴリは連続回で偏らないよう
ローテーション管理する（SCの「era偏りに注意」と同じ発想）。

| カテゴリ | 範囲 |
|---|---|
| 高齢化・介護・自立支援 | 見守り・移乗介助・コミュニケーションロボット等 |
| 家庭内ロボット・家事自動化 | 家事タスク・生活支援デバイス |
| 医療補助技術 | **AI/ロボットによる支援技術のみ**（リハビリ機器・モニタリング等）。診断・治療そのものの医学的妥当性を論じる回は作らない（医師免許保持者が強い「医学全般」領域には踏み込まない、企画書の避けたジャンル方針） |
| モビリティ・身体拡張 | エクソスケルトン等 |
| 働き方の変化 | AIエージェントによる業務変化等 |
| 防災・安全 | レスキューロボット等 |

**自動プレフィルタ（人間が見る前に機械的に削る）:**
1. `year >= 現在-3`（鮮度基準）
2. `publicationTypes` に Review / Editorial / LettersAndComments を含む論文は除外
   （独自データを持たないレビュー・サーベイをAPIパラメータで機械的に弾く）
3. `venue` が既知の質の低い誌に該当しないか確認する。未知の誌は自動除外せずSTAGE2で
   人間確認に回す（取りこぼしより誤除外の方が危険なため許可リスト方式・厳格すぎるブロックリストにはしない）
4. `topics_queue.json`（今後作成）と照合し、既に検討済み・既出の論文を重複排除
5. **スコアによる足切りはしない（2026-08撤回）。** 機械的フィルタ（1〜4）を通過した
   全件をSTAGE2へ渡す。旧ルールは「引用数×新しさの複合スコアで上位8件程度に絞る」
   だったが、発表直後で被引用数がまだ積み上がっていない最先端論文がこのスコアで
   不当に低く評価され、変革ポテンシャルをSTAGE2/4のGeminiが判断する前に機械的に
   取りこぼすリスクがあると判明したため撤回した。スコアは並び順の参考にのみ使う

**カテゴリ重み（weight）とアナリティクス連動:** `query_vocabulary.json` の各カテゴリに
`weight` を持たせ、STAGE1実行時のカテゴリ登場頻度に反映する。ただし**公開エピソードが
最低12話に達するまではweightを全カテゴリ均等のまま固定する**（lamp-whisperの
「n≈12まで待つ」ルールを踏襲。少数サンプルのノイズで特定カテゴリを早合点で切り捨てない
ため）。12話到達後は `kl_analytics_report.py`（今後作成）の分析結果を踏まえ、人間が
手動でweightとクエリ語彙を見直す。自動での語彙生成・自動weight更新は行わない。

**鮮度:** 直近1〜3年を基本とする。

### STAGE 2 — 信頼性チェック（採否フィルタ）

**2026-08改訂: 「査読済みのみ」ルールは撤回した。** 当初は話題性より確実性を優先する
狙いで査読完了を必須としたが、kl001（Robi Butler）で実際に台本化した結果、
「安全だが地味」な研究に寄り、企画書が掲げる「信頼できる研究が暮らしを劇的に変える」
という面白さを失うことが判明した。現在ヒューマノイド量産化等が話題になっている
時期に、査読待ちの間に現実が追い越してしまう分野だと再認識したため撤回する。

- **査読済み論文は引き続き信頼性シグナルの1つとして評価するが、必須条件ではない。**
  未査読プレプリントも、以下の信頼性シグナルで判断する:
  - 著者所属機関の実績（DeepMind / Stanford / MIT / CMU / Berkeley / NUS等の主要大学・
    研究所、Figure AI / 1X / Boston Dynamics等の実績あるロボティクス企業の研究部門など）
  - 技術的検証の厳密さ（定量的なベンチマーク・比較評価があるか、単なる宣伝資料でないか）
  - 著者・研究室の過去実績（同分野での既発表実績があるか）
  - この判断はGemini（STAGE2スクリプト `kl_paper_screen.py`）に、掲載誌に加えて
    著者所属・技術的厳密さも検索させたうえで委ねる（`venue_assessment` の
    `reputable/unknown/low_quality` 判定に、プレプリントの場合はこれらのシグナルを
    加味させる）
  - **コスト最適化（2026-08確定）:** IEEE/ACM/Nature/Science/JMIR/PLOS/主要ML・
    ロボティクス会議（ICRA・IROS・NeurIPS・ICML等）・経済学トップジャーナルなど
    明らかに信頼できる出版社・学会に掲載された非プレプリント論文は、Gemini呼び出しを
    スキップして自動passする（許可リストは `kl_paper_screen.py` の
    `REPUTABLE_VENUE_KEYWORDS`）。**許可リストに載っていないことを自動excludeの
    理由にはしない。** プレプリントはリストの有無に関わらず必ずGeminiの個別レビューに
    回す（許可リスト外＝即除外にすると、プレプリント全体を機械的に締め出すことになり、
    上記の「査読済みのみルール撤回」の意図を別の形で無効化してしまうため）
- サンプルサイズ・実験規模を確認し、極端に小さいn数の場合は採用を見送る
- 単発研究のみか、複数の独立研究で類似結果が追試されているかを確認する
- 資金源・利益相反（企業自主資金研究かどうか）を確認し、該当する場合は開示する
- **査読前の研究を扱う場合の開示先は概要欄とする（2026-08-20改訂）。**
  「査読」「プレプリント」は一般視聴者に馴染みのない専門用語のため、ナレーション内
  では使わない。出典の詳細（査読済み/査読前の別を含む）は概要欄の参考文献欄に記載し、
  そこで検証可能性を担保する。ナレーション内では「まだ研究段階のプロトタイプ」
  「実用化にはまだ時間がかかりそう」といった、専門用語を使わない一般的な表現で
  完成度の低さ・不確実性を伝えれば足りる（企画書の「誠実な解説」方針との整合。
  STAGE3の誇張チェックがこの層の主な安全網になる）

**既知の制約（2026-08確認・kl002で発覚）:** Semantic Scholarは同一論文のプレプリント版
（arXiv/SSRN等）と正式出版版を別レコードとして統合していないことがある。この場合、
査読済み出版版がSemantic Scholar上に存在せず、プレプリント版のvenue表記だけで
STAGE1の自動除外にかかってしまう。該当論文が別ルートで見つかった場合は、
出版元サイト（学会・出版社の公式ページ）のURLで査読済み掲載を人間が直接確認できれば、
`topics_queue.json` に `pipeline_gap_flag: true` を付けて例外的に採用してよい。

### STAGE 3 — 誇張表現の検出（Consultのアドバーサリアル役）

- 論文本文の実際の主張と、二次報道・プレスリリースの煽り表現とのギャップを確認する
- 「〜の可能性がある」「〜傾向が見られた」といったヘッジ表現が、二次情報で
  断定表現に変わっていないか確認する
- Gemini（`/consult`相当）にアドバーサリアル役（懐疑的レビュアー）を明示的に指示して委譲する

### STAGE 4 — 「面白いか」の一次判定

- **変革ポテンシャル・野心度**（2026-08追加、最重要視する）: この技術が本当に広く
  実現した場合、暮らしをどれだけ劇的に変えるか。「すでに確立された地味な改善」より
  「まだ実現していないが実現すれば劇的」を積極的に評価する。企画書の核心
  （信頼できる"先取り"）に最も直結する観点のため、他の観点より重く重み付けする
- 生活実感との直結度（視聴者が「自分ごと」として想像できるか）
- 数字のインパクト（意外性のある定量的結果があるか）
- 「使い捨ての生活者」ペルソナに具体的な生活シーンとして落とし込みやすいか
- 直近エピソードとサブジャンルが連続して被っていないか（STAGE 1のローテーションと連動）

### STAGE 5 — 最終ゴーサイン

- 上記4段階の結果を踏まえ、**人間（なるさん）が最終判断する。** STAGE 2の❌
  （査読前・信頼性不足）は自動除外でよいが、STAGE 3/4は人間確認を必須とする。

---

## コンテンツ設計（エピソードJSON）

エピソードの正データは Samurai Chronicles と同様、構造化されたJSON
（`episodes/kl{NNN}.json`、今後作成）とする。LWのようなフリーフォーム
テキスト台本ではなく、`kl_video_gen.py` 等のスクリプトが直接読める構造化データを
正とする方式（2026-08確定）。

### 動画尺・シーン数（2026-08確定）

企画書の「静止画10枚程度」は初期の企画メモの数字で、動画尺・ナレーション分量からの
逆算ではなかった。実際に必要な構成要素から逆算し、以下に確定する。

- **目標動画尺: 7〜9分**（2026-08-20改訂。当初6〜8分としたが、YouTubeは維持率%より
  総視聴時間（分）で評価される傾向があり、5〜6分では総視聴時間で不利になりやすいと
  判断し引き上げた。単発論文回は7分寄り、複数論文を組み合わせる回は9分寄りを目安にする）
- **シーン数: 10〜15シーンの可変レンジ**（固定10枚ではない。2026-08-20改訂、
  当初10〜13としたが、複数論文回で「生活への影響」を複数の具体的な情景に
  分けてビジュアル化する場合は15シーン程度まで許容する）。SCと同じく、
  クリップ尺は実際のナレーション音声の長さから決定する方式とし、
  シーン枚数を先に固定しない
- 内訳の目安: フック1 / 出典明示1 / 論文の背景・課題設定1〜2 / 論文の内容・手法・結果
  2〜3 / グラフ・比較図1〜2（企画書の画配分方針を踏襲） / 生活への影響2〜5 / 締め1
  （複数論文回は「論文の内容・手法・結果」を論文数に応じて増やす。「生活への影響」も
  1つの抽象的なまとめ文で終わらせず、朝・夕方・休日のような複数の具体的な瞬間に
  分けて描くとビジュアル的な説得力が増す）

### シーンタイプ体系とBGM3曲構成の対応（2026-08確定）

上記の内訳をそのまま `scene.type` の値にする。`kl_video_gen.py`（今後作成）は
SCの `sc_video_gen.py` と同じ考え方で、シーンタイプの並びからBGM3曲
（序盤intro／中盤main／終盤outro）の切り替え境界を自動計算する。

| type | 内容 | BGM役割 | 画風 | ナレーター |
|---|---|---|---|---|
| `teaser` | 冒頭の離脱回避ティザー（本編の`impact`シーンの一部を数秒先出し） | intro | 物語調イラスト | 生活者ボイス |
| `hook` | 主人公の自己紹介（3〜5秒） | intro | 物語調イラスト | 生活者ボイス |
| `citation` | 出典明示（5〜10秒、大学名・発表年等） | intro | 物語調イラスト | 研究ボイス |
| `context` | 主人公が抱える課題の説明 | main | 物語調イラスト | 生活者ボイス |
| `finding` | 課題を解決しうる研究の紹介（内容・手法・結果） | main | 物語調イラスト | 研究ボイス |
| `data` | グラフ・比較図・フローチャート | main | チャート調（BASE_CONTEXTと別トーン） | 研究ボイス |
| `impact` | それが実現した未来の暮らし（主人公視点） | outro | 物語調イラスト | 生活者ボイス |
| `closing` | 締め | outro | 物語調イラスト | 研究ボイス |

**境界計算ルール**（SCの `rising_action`/`climax` 境界判定と同じ考え方）:
- 境界1（intro→main）: 最初の `context` または `finding` シーンの開始位置
- 境界2（main→outro）: 最初の `impact` シーンの開始位置

`data` タイプのシーンは、企画書の画配分方針（1〜2枚のグラフ・比較図）に対応する。
`kl_image_gen.py`（今後作成）は `data` タイプの場合、物語調イラストのBASE_CONTEXTではなく
チャート・フローチャート向けの別プロンプトテンプレートを使う。

### BGMパイプライン（2026-08-21追加）

samurai-chroniclesのBGMパイプライン（Freesound検索→音声QA→ライブラリ管理→
実音声での最終チェック→CC BYクレジット注入）をkagaku-life向けに移植した。
動画生成（`kl_video_gen.py`、クロスフェード合成部分）自体は未実装のため、
現時点では候補選定〜ライブラリ登録までが対象。境界計算・クロスフェードの
設計はSCの `sc_video_gen.py` と同じ考え方を踏襲する想定（上記「境界計算
ルール」参照）。

**BGMルール（SCとは異なる、kagaku-life独自のトーン）:**
- **必ず温かく・希望が持てる・押し付けがましくないトーン**に限定する
  （SCの「重厚なオーケストラ調」路線はこのチャンネルには合わない —
  番組の核が「科学が主人公の暮らしにもたらす小さな幸せ」であるため）
- クエリに `warm` / `gentle` / `hopeful` / `uplifting` / `heartfelt` /
  `cozy` のいずれかを含めることを推奨する
- 禁止: epic, battle, war, dark, aggressive, horror, dramatic tension
  （壮大・攻撃的・悲壮すぎる曲は番組のトーンと合わない）
- 役割とトーンの対応:

| 役割 | 対象シーン | トーン | クエリ例 |
|---|---|---|---|
| intro（序盤） | teaser〜citation | 好奇心・静かな導入 | `"curious piano"` |
| main（中盤） | context〜data | 共感できる悩み〜研究紹介への高まり | `"warm uplifting"` |
| outro（終盤） | impact〜closing | 温かい余韻・小さな幸せ | `"heartfelt strings"` |

**構成スクリプト:**
- `kl_bgm_qa.py` — BGM候補の音声QA（Geminiの音声理解でボーカル・台詞混入を
  自動検出。SCの `sc_bgm_qa.py` と同じ仕組み）
- `kl_bgm_library.py` — BGMライブラリ（`bgm_library.json`）管理。新規BGMの
  登録・エピソードへの紐付け（役割別 `bgm_sources[role]`）
- `kl_inject_bgm_credit.py` — CC BY曲のクレジットを `youtube_description` に自動注入
- `kl_bgm_final_check.py` — 選定した3曲の実音声＋制作確認書をGeminiに渡し、
  トーン適合・3曲の流れ・音質を最終判定（SCの音楽監督プロンプトをkagaku-life
  向けに書き換え）
- Freesoundからのダウンロード自体は lamp-whisper由来の共有スクリプト
  `$HOME/lamp-whisper/freesound_download.py` をそのまま流用する
  （`--library $HOME/kagaku-life/bgm_library.json` を必ず指定し、
  kagaku-life独自のライブラリで重複を判定させる）

**実行例:**
```bash
mkdir -p "$HOME/Desktop/kagaku-life/{episode}/BGM"
FREESOUND_API_KEY=$FREESOUND_API_KEY python3 "$HOME/lamp-whisper/freesound_download.py" \
  "<Q_intro>" "<Q_main>" "<Q_outro>" \
  "$HOME/Desktop/kagaku-life/{episode}/BGM/" \
  --round 0 --start-slot 1 \
  --library "$HOME/kagaku-life/bgm_library.json"

# スロット→役割プレフィックスへのリネーム後
python3 kl_bgm_qa.py --dir "$HOME/Desktop/kagaku-life/{episode}/BGM"
```

**動作確認（2026-08-21、kl001で実施）:** 上記の流れで実際に3曲
（intro/main/outro）をダウンロードし、`kl_bgm_qa.py` で全曲ボーカルなしを
確認した。1回のクエリで0件になるケースがあり（Freesoundの30〜300秒・
CC0/Attributionフィルタに合致する曲が少ない）、SCと同様に該当スロットだけ
`--start-slot` で別クエリに差し替える運用が必要だった。テスト用にダウン
ロードした音声ファイルはgitにコミットしない（Desktop側で確認用）。
最終選定・ライブラリ本登録（`kl_bgm_library.py --add`）は、実際に聴いて
ユーザーの承認を得てから行う。

**候補の絞り込み方法（2026-08-21確定、SCの現行方式を踏襲）:** SCも
2026-08-04以降は`/consult`を使わず、**Claude（オーケストレーター自身）が
テキスト情報（曲名・タグ・`kl_bgm_qa.py`の音の説明）だけで役割ごとに
複数候補から1曲に絞り込み、実音声での検証は`kl_bgm_final_check.py`に
任せる**方式に変更している（BGM選定はオープンな戦略相談ではなく
タグ・制約への照合作業のため、`/consult`ほどの重さは不要と判断された）。
kagaku-lifeも同じ方式を採用する。

**テキストだけの絞り込みでは不十分な例（2026-08-21、kl001で実際に発生）:**
役割ごとに複数候補（各役割2〜3曲）をFreesoundから集め、曲名・タグを見て
introに "Atonal Crystal Chime"（風鈴・グロッケンで静かな曲、と判断）を
選んだが、`kl_bgm_final_check.py`の実音声チェックで「無調でSF的・冷たい
印象、チャンネルの温かいトーンと乖離」と判定された。テキスト情報だけでは
実際の質感（無調＝atonalの響き等）を見誤ることがあるため、**実音声での
最終検証（`kl_bgm_final_check.py`）を省略しない。** 差し替え後の再検証で
「温かみのあるアコースティックピアノ／ギター」に変更し、intro/mainは
「適合」、outroは「良曲だがやや情緒的すぎる可能性、ダッキング音量を
2〜3dB下げるか差し替えを検討」という条件付き採用の判定を得た——
このように**「一部だけ差し替え」の部分的なフィードバックが出ることも
想定し、3曲まとめてではなく役割単位で差し替え・再検証できるようにしておく**。

**`finding` シーンで成果の数値に触れる場合の画像表現（2026-08確定）:** ナレーションで
具体的な成功率等の数値を紹介する場合、image_promptにも反映して良いが、**AI画像生成は
数字・文字を正確に描けないため、パーセント等の数字テキストを直接指定しない**
（他のシーンと同じ "no text" 方針の一環）。代わりに、棒の高さ・円の塗りつぶし度合いなど
「割合が視覚的にわかるが数字は読めない」比較グラフィックをシーンの一部（コーナー要素等）
として指定する（例: before/afterで棒の高さが変わる、円グラフの塗りが増える、等）。

**研究紹介での所属機関・研究者名の扱い（2026-08確定）:** 信頼感を高めるため、
`finding`/`citation` シーンでは可能な限り実際の所属機関名（大学・企業名）を
ナレーションに明記する。ただし**研究者個人の人名（特に外国人名）はナレーションに
含めない**——日本語TTSでの発音が不自然になりやすいため。人名は `references` の
`lead_researcher` フィールドに記録し、概要欄・制作確認書でのみ表示する。

### ストーリー構成と2ナレーターボイス制（2026-08確定）

企画書の「物語調」演出を徹底するため、単なる論文解説ではなく以下の構成で語る。

0. 冒頭の離脱回避ティザー（`teaser`、2026-08-21追加）
1. 主人公の簡単な自己紹介（`hook`）
2. 主人公が抱える課題の説明（`context`）
3. それを解決しうる研究の紹介（`citation` / `finding` / `data`）
4. それが実現したときの未来の暮らし（`impact`）
5. 締め（`closing`）

**`teaser`について（2026-08-21追加）:** samurai-chroniclesが冒頭に離脱回避
ティザーを設けているのに倣い、本編で一番「面白い部分」（`impact`シーンの
未来の暮らし）が動画の後半まで出てこない構成による序盤離脱を防ぐため追加。
`hook`（自己紹介）に戻って本編を始める前に、`impact`シーンの内容を数秒だけ
先出しする。ナレーターは主人公自身の未来を見せる形になるため生活者ボイス。
`image_prompt`は先出しする`impact`シーンと同じ内容を指定してよい
（`kl_video_gen.py`実装時に同一素材を使い回すか、別途生成するかは
そのとき判断する）。

**複数カットの高速フラッシュ構成にする（2026-08-21改訂）:** 当初は`teaser`を
1シーン（約8秒、単一の未来カット）としていたが、8秒を2秒×4カットに分割し、
複数の異なる`impact`シーン（朝・夕方・週末など）を矢継ぎ早に見せてから、
最後に短い問いかけで締める構成に変更した。1カットだけよりテンポが出て
「引き」が強くなる。2秒の尺に収めるため、各カットのナレーションは10〜15字
程度の短いフレーズに抑える（日本語ナレーションは概ね4〜5字/秒。目安は
`kl_confirmation_doc.py`の`estimate_seconds`と同じ250〜300字/分）。最後の
締めのカットだけは問いかけの完結のため少し長め（4秒程度）でもよい。

**ナレーターは2声を使い分ける。** 主人公パート（1・2・4）は主人公と同性の
「生活者ボイス」、研究解説パート（3）は主人公と異性の「研究ボイス」を使う
（例: 主人公が女性なら研究解説パートは男性ボイス）。物語部分と研究解説部分を
声で切り替えることで、聞き手にも構成の切り替わりが自然に伝わる。

**締め（`closing`）は研究ボイスに戻す（2026-08-20確定）。** `citation`（研究ボイス）で
開き、`closing`（研究ボイス）で締めることで、物語の内側（生活者パート）を客観的な
語りが挟み込む構成にする。生活者パートが一人称の独白調になるため、最後だけ視点が
戻らないと「誰が語っているか」が曖昧になりやすいという理由もある。

**生活者パートは一人称の語り口にする（2026-08-20確定）。** `hook`/`context`/`impact`
は「田中美咲さん、34歳。〜そんな日々を送っています」のような三人称の説明調ではなく、
「34歳。〜そんな毎日です」のように主人公自身が語っている一人称の独白として書く
（主語の「私」は必要な箇所以外は省略してよい、自然な日本語の一人称語りを優先する）。
`citation`/`finding`/`data`/`closing`（研究ボイス）は引き続き客観的な説明調で書く。

**`impact` の未来描写は「想像している」ことを毎回明言せず、その場にいる感情として
描く（2026-08-20確定）。** `impact` は「もし実現したら」という前提をシーン冒頭で
1回示せば十分で、以降のシーンで「〜かもしれません」「そんな想像が頭をよぎります」
といったヘッジ表現を繰り返す必要はない。むしろ未来ドラマの中に入り込んで、その状況を
心から喜んでいる・安堵している、という没入感のある感情表現を優先する（例:
「コーヒーを味わえる——そんな余裕が、うれしくてたまりません」）。ヘッジを重ねすぎると
説明的になり、主人公自身の生きた感情から遠ざかる。

**全エピソード共通の「オチ」（2026-08-20確定）: 科学の本当の価値は、それが
主人公の暮らしにもたらす小さな幸せである、という着地点で終える。** 技術そのものの
すごさ（性能・数値・野心度）はSTAGE4で評価する選定基準ではあっても、エピソードの
感情的な締めくくりにしてはいけない。`impact`シーンで描く朝・夕方・休日のような
具体的な瞬間は、単なる機能紹介の羅列ではなく、主人公がそこで感じる小さな喜び・安堵・
愛おしさそのものが主題である。`closing`（研究ボイス）は技術の現在地（まだ研究段階等）
を正直に示しつつ、その先に主人公の暮らしがあるという着地に触れて締める。kl001
（朝のコーヒー・家族の食卓・娘との週末）がこの型の実例。

`protagonist.gender` から生活者ボイスの性別を決め、研究ボイスはその逆に固定する。
`kl_tts_gen.py`（今後作成）が `scene.narrator` を見てGemini TTSのボイス名を
切り替える想定。

**ボイス選定方針（2026-08確定）:** モデルは `gemini-3.1-flash-tts-preview`。
生活者ボイスと研究ボイスで扱いが異なる。

- **生活者ボイス（persona）: 毎回選び直す。** 主人公は毎回別人であり、同じ
  性別でも声を使い回さず、エピソードごとに聴き比べて改めて選定する。
  チャンネル共通の固定ボイスにはしない。
- **研究ボイス（research）: 性別ごとに一度決めたら固定する。** 研究解説
  パートは主人公とは独立した「客観的な語り手」の役割のため、同じ性別が
  必要な限り毎回同じ声を使う。**男性研究ボイスは `Orus` に確定した
  （2026-08、kl001で決定）。** 女性研究ボイスはまだ未選定で、女性研究
  ボイスが必要になる回（主人公が男性の回）で初めて選定し、以降は
  同様に固定する。

**kl001での実績:** 女性候補5種（Kore/Leda/Autonoe/Despina/Sulafat）・男性候補
5種（Charon/Orus/Iapetus/Rasalgethi/Achird）をkl001の実ナレーション文で読み
上げ比較し、生活者（田中美咲）=`Leda`（このエピソード限定の選定）、
研究=`Orus`（以降固定）に決定した。

**複数論文を扱う回について**（2026-08追加。企画書「複数論文を扱う回は、該当箇所
ごとに簡易テロップを併用」に対応）: `references` 配列に複数エントリを持たせ、
各 `finding`/`data` シーンに `reference_index`（`references` 配列のインデックス、
0始まり）を付けて、どのシーンがどの論文の紹介かを紐づける。単一論文の回では
`reference_index: 0` で固定でよい。

### 画像スタイル（2026-08-21改訂）

`gemini-3.1-flash-image` で実際に複数テイストを比較検証し、以下2種のBASE_CONTEXTに
確定した。`kl_image_gen.py`（今後作成）はシーンの `type` に応じて使い分ける。

**物語シーン用（`hook`/`citation`/`context`/`finding`/`impact`/`closing`）:**
```
Rich flat editorial illustration style with soft warm lamp/window lighting and a
subtle grain/noise texture overlay: naturalistic character proportions and skin
tones, gentle shading gradients rather than flat cel-shading. Muted, sophisticated
palette (slate blue, teal, warm gray) with one warm coral/amber accent light
glowing softly within the scene. Cozy, intimate, magazine-editorial illustration
quality. Not photorealistic, no anime style, no harsh dramatic light rays.
Characters have authentically Japanese facial features (this is a
Japanese-audience channel) — not Western or ambiguous.
```
**改訂の経緯:** 当初は「シネマティックな光（強い指向性の光・深い陰影）」を採用した
が、実際にkl001本編のシーンで運用してみると演出が強すぎてクセがあると判断し、
より落ち着いた「暖色グレイン」路線（柔らかいランプ光・粒子感のあるテクスチャ・
控えめな陰影）に差し替えた。あわせて、以前のテストで日本人ではなく欧米人の
顔立ちで生成される問題が見つかったため、キャラクターの容姿を明示的に指定する
一文を追加した。ミニマル路線・紙工作風・ソフト3D路線・完全にフラットで陰影の
ない「無印リッチフラット」は比較の結果不採用（無印はクセがない一方、単調で
安っぽく見えたため）。

**`data` タイプ専用（物語シーンとは別トーン）:**
```
Simple, clean, minimalist flat infographic bar chart — like a basic data
visualization slide, NOT an artistic or abstract illustration. Plain solid flat
colors only, no dramatic lighting, no light rays, no gradients, no texture, no
decorative background shapes. Cool color palette (deep blue, teal) for baseline
values, one warm coral/orange color for the improved/highlighted value.
ABSOLUTELY NO TEXT: no letters, no words, no numerals, no dates, no labels
anywhere in the image — convey all meaning through icon shape, bar height, and
position only. If you would normally add a caption or axis label, omit it
entirely and leave that space blank.
```
**なぜ物語シーンと同じ演出的なスタイルを流用しないか:** 検証時、物語シーン用の
リッチフラット・シネマティックライトのどちらのスタイルでも、比較棒グラフが
「アート作品」寄りになりすぎて、パッと見て数値の大小が伝わりにくくなる問題が
確認された（雰囲気重視のスタイルとデータの視認性はトレードオフになりやすい）。
`data` タイプは意図的に演出を抑え、視認性を最優先したチャート専用スタイルとする。

**「no legible text」だけでは不十分、明示的な禁止文が必要（2026-08-21追加）:**
kl001の実生成テストで、「no legible text or numerals」という控えめな表現では
AI画像生成が英語のラベル文字（年号・タスク名等）をはっきり描画してしまう
問題が確認された。「ABSOLUTELY NO TEXT」のように強く明示し、「キャプションを
入れたくなってもその余白は空白のままにする」旨まで書くことで解消した。

**CHART_CONTEXTが「bar chart」と決め打ちしていると、シーン固有の構図が
上書きされる（2026-08-21追加）:** 「同じ壁に複数チームが別角度から挑む」
という`data`シーン（壁＋矢印＋トレンド線の構図をimage_promptで指定）が、
何度再生成しても単純な棒グラフに戻ってしまう問題が発生した。原因は
CHART_CONTEXT側が冒頭で「flat infographic **bar chart**」と明言していた
ため、シーン固有の指示より強く効いてしまっていたこと。CHART_CONTEXTを
「diagram or chart」という汎用的な表現に変更し、「シーンの記述に具体的な
図の種類が書かれていればそれに従う（bar chartとは限らない）」と明記して
解消した。`data`タイプのシーンで棒グラフ以外の構図（矢印・収束点等）を
使う場合は、この汎用化されたCHART_CONTEXTが前提になる。

**`finding` シーン内の「コーナー要素」も視認性を優先する（2026-08-21追加）:**
`finding` タイプのシーンでナレーションの数値を反映する棒グラフ・比較リング等の
コーナー要素（上記「finding シーンで成果の数値に触れる場合の画像表現」参照）は、
シーン全体はBASE_CONTEXTの演出的なスタイルで描きつつ、要素自体は
`data` タイプに近いシンプルなフラット表現に留める旨をimage_promptに明記する
（例: "a small flat corner graphic — no larger than roughly one-eighth of the
frame, tucked unobtrusively in a corner, kept plain and contrasting with the
richer main illustration for legibility, absolutely no text or numerals"）。
kl001の初回テストでは「small」とだけ書いたコーナー要素が画面の1/4近くを
占める大きな白いポスターのように浮いてしまったため、「1/8以下」という
具体的なサイズの目安を明示するよう改訂した。演出と視認性のトレードオフは
`data` タイプ単体だけでなく、`finding` シーンの装飾要素にも同様に起こりうる
ため。

**時間帯の指定は明示する（2026-08-21追加）:** 「morning」「早朝」等の時間帯を
言葉で示すだけでは、AI画像生成が誤って夜空（月・星）を描くことがあると
kl001の検証で判明した。`image_prompt`では時間帯を視覚的に明示する
（例: "soft morning daylight coming through the window (not night — clearly
bright dawn light)"、"evening — warm interior lighting with a dusk sky visible
through the window"）。

### 画像QA（2026-08-21追加）

samurai-chroniclesの `sc_image_gen.py` の画像QA（Gemini Vision）と同じ考え方で、
`kl_image_gen.py` に自動QAを組み込んだ。それまでの不具合（ロボットの顔が人間的、
複数人物が同一人物に見える、文字混入等）はすべて人間の目視確認頼みだったため。

**流れ:** 生成 → `gemini-3.6-flash` の画像理解でQAチェック → NGならQAの指摘内容を
プロンプトに追記して自動再生成（最大2回試行。SCの実績データで3回目以降は
効果が薄いことが判明していたため2回に設定）。それでも解決しなければ、その旨を
表示してユーザーの目視確認に委ねる。

**QAカテゴリ（SCの時代考証・髪型カテゴリの代わりに、kagaku-lifeで実際に
繰り返し起きた問題をカテゴリ化）:**
- `MISMATCH` — シーン内容と食い違う
- `DISTORTION` — 手・顔・身体の歪み
- `TEXT` — 読める文字・数字・透かしが写り込む
- `STYLE` — 意図した画風（物語調イラスト／チャート）と異なる
- `FACE` — 研究用ロボットの頭部が機械的であるべきなのに人間的な顔になっている
- `DUPLICATE_PERSON` — 別人であるべき複数人物が同一人物に見える

**動作確認（2026-08-21、kl001のS9で実施）:** 実際にQAが「研究者がアメリカ人
という指定に反して東アジア系に見え、タブレットも持っていない」という
MISMATCHを検出し、2回目の自動再生成で解消することを確認した。

`--no-qa` オプションで従来通りQAなしの生成もできる（QA自体もGemini APIの
追加コストがかかるため、大量の一括再生成で確認目的だけの場合など）。
QA結果は `~/Desktop/kagaku-life/{episode}/image_qa_result.json` に保存する。

### エピソードJSONフォーマット（`episodes/kl{NNN}.json`）

SCのフォーマットを土台に、`references`（出典）と `protagonist`（使い捨ての生活者）
を追加した。ティザー（`teaser`シーンタイプ）は上記の通り本編`scenes`に組み込む形。
Shorts（`shorts`フィールド）は2026-08-21に追加した（経緯は下記）。

```json
{
  "episode_id": "kl001",
  "episode_title": "...",
  "youtube_title": "...",
  "youtube_description": "...",
  "youtube_tags": [...],
  "references": [
    {"title": "...", "authors": ["...", "..."], "year": 2024, "venue": "...", "url": "..."}
  ],
  "protagonist": {"name": "...", "age": 0, "job": "...", "gender": "female"},
  "narration_voices": {"persona": "...", "research": "..."},
  "thumbnail_prompt": "...",
  "scenes": [
    {
      "scene_id": 1,
      "type": "teaser",
      "narrator": "persona",
      "duration_seconds": 8,
      "narration": "...",
      "image_prompt": "...",
      "ken_burns": "zoom_in"
    },
    {
      "scene_id": 2,
      "type": "hook",
      "narrator": "persona",
      "duration_seconds": 5,
      "narration": "...",
      "image_prompt": "...",
      "ken_burns": "zoom_in"
    },
    {
      "scene_id": 4,
      "type": "finding",
      "narrator": "research",
      "reference_index": 0,
      "duration_seconds": 20,
      "narration": "...",
      "image_prompt": "...",
      "ken_burns": "zoom_in"
    }
  ],
  "shorts": [
    {
      "shorts_id": 1,
      "scenes": [
        {"narrator": "persona", "narration": "...", "image_prompt": "..."},
        {"narrator": "research", "narration": "...", "image_prompt": "...", "style": "chart"},
        {"narrator": "persona", "narration": "...", "image_prompt": "..."}
      ]
    }
  ]
}
```

`duration_seconds` はSCと同じくフォールバック専用（音声なしシーン用）で、
音声ありシーンの実クリップ尺は生成されたナレーション音声の長さから決定する。
`references` は `topics_queue.json` の該当エントリ（title/authors/year/venue/url）を
そのまま引き継いで生成する（複数論文回は複数エントリ）。`protagonist` も
`topics_queue.json` の該当エントリにSTAGE4で生成済みの値があればそれを引き継ぐ。
`narrator` は `persona`/`research` の2値、`reference_index` は `finding`/`data`
シーンにのみ付与する（上記「ストーリー構成と2ナレーターボイス制」参照）。

**Shorts（`shorts`フィールド、2026-08-21追加）:** samurai-chroniclesが販促目的で
Shortsを別立てで作っているのに倣い追加した。当初は企画書に記載がないという理由で
除外していたが、離脱防止ティザーと同じ理由（チャンネルの認知拡大）で採用する
価値があると判断し撤回した。

- **既存の横長(16:9)本編画像からの切り出しは採用しない。** 本編の構図は
  キャラクターが画面の左右どちらかに寄って配置されることが多く、9:16で中央だけ
  切り出すと人物やグラフの一部が欠けることを実際の画像で確認したため。
- 各エピソードにつき `shorts` 配列（通常1本）を持たせる。**各カットは
  ティザーと同じく2〜3秒程度の短いフレーズに区切り、枚数を多め（5〜6枚
  程度）にしてテンポを出す**（2026-08-21改訂。当初3シーン・1文ずつの構成
  にしたところ1カットが4〜6秒と間延びしたため、短いフレーズに分割して
  枚数を増やした）。全体で「フック→本編のダイジェスト→CTA」という
  短い展開にする。**`image_prompt` はShorts専用に縦構図を意識して新規に
  書く**（本編のシーンを流用する場合も、9:16で人物が中央に収まるよう
  文言を調整する）。
- `kl_image_gen.py` は `shorts` 配下のシーンを生成する際、
  `types.ImageConfig(aspect_ratio="9:16")` を指定する（`gemini-3.1-flash-image`は
  `image_config.aspect_ratio` でアスペクト比を直接制御でき、実際に768×1376
  （ほぼ9:16）で生成されることを確認済み）。本編・サムネイルは指定なし
  （モデルのデフォルトの横長比率、実測1408×768）のまま。
- **チャート系のカットには `"style": "chart"` を付与する（2026-08-21追加）。**
  Shortsのシーンは`type`を持たないため、`kl_image_gen.py`はデフォルトで
  常にBASE_CONTEXT（物語調）を使う。棒グラフ・図解等のカットにこの指定を
  忘れると、本来CHART_CONTEXTで描くべき図がBASE_CONTEXTの演出的な
  スタイル（部屋の背景等）に埋もれてしまう問題が実際に起きたため、
  `style: "chart"` があればCHART_CONTEXTを使うようにした。
- `narrator`・`narration_voices` は本編と共通のものを使う。
