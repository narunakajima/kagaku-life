# STAGE4 transformation_score 採点ロジック改修 — 引き継ぎメモ

作成: 2026-08-28（別セッションで論文抽出パイプライン実行中のため、作業引き継ぎ用に作成）

## 背景

`kl_paper_interest_score.py`（STAGE4「面白いか」一次判定）で「変革ポテンシャル・
野心度」を最重視するよう2026-08に改訂したが、昨日それを踏まえて論文抽出を
全面的にやり直しても、依然として「聞いたことがあるような」地味な研究が上位に
来てしまう問題が残っている。

**具体例:** home_robotカテゴリの "Robi Butler: Remote Multimodal Interactions
with Household Robot"（arXiv, 2024）が `T5/L5/S3/P5=4.5`
（transformation_score=5 満点）という高スコア。しかし「外出先からスマホで
ロボットに家事を指示する」という体験自体は、ロボット掃除機・スマート家電で
既に一般的であり、transformation_score 5点（満点）は過大評価ではないか、
という指摘があった。

## 診断

現在の`PROMPT_TEMPLATE`内のtransformation_score定義は「実現したら暮らしを
どれだけ劇的に変えるか」を単発で聞くだけで、**「現在すでに市販製品で実現して
いることと何が違うか」を明示的に比較させる指示がない**。論文のアブストラクトは
野心的な書き方をされがちなため、比較基準なしで採点するとLLMは実際には既存
技術の焼き直しであっても高得点をつけやすい（これは査読チェック＝STAGE2や
誇張チェック＝STAGE3とは別の盲点。あちらは「論文の主張 vs 二次報道の誇張」を
見るが、こちらは「論文の主張 vs 現在の市場の現実」を見ていない）。

## 合意した対処方針（未実装）

1. transformation_scoreのプロンプトに、「現在の消費者向け製品（スマート家電・
   ロボット掃除機等）で既に実現していることと比較し、"本当に新しい部分"のみを
   評価する」という指示を追加する。
2. 現状「創作寄りの判断だからGoogle Search groundingは不要」としている
   （`kl_paper_interest_score.py`冒頭のコメント参照）が、"何が既に市場に
   存在するか"は検索で確認すべき事実判断なので、**transformation_scoreの
   判定に限りSearch groundingを有効化**する。
3. （会話中で提案したのみ、明示合意はまだ）reasoningフィールドに「何と比較して
   何が新しいと判断したか」を明記させ、STAGE5での人間レビュー時に採点根拠を
   検証しやすくする。1・2を実装する際の自然な追加項目として提案していた。

## 未着手（このメモの時点でコード変更なし）

- `kl_paper_interest_score.py` の `PROMPT_TEMPLATE` 書き換え
  （transformation_score部分に比較指示を追加）
- Gemini呼び出しにsearch groundingツールを追加する必要があるか確認・実装
  （現在`score_paper()`は`client.models.generate_content(model=MODEL, contents=prompt)`
  のみでgrounding未使用）
- 修正後、`stage3_hypecheck.json` に対してSTAGE4を再実行
  （対象件数・API呼び出しコストは実行前に確認すること。全カテゴリ横断で
  228件処理という記述がスクリプトのdocstringにあり）

## 関連する別の議論（このセッションで話しただけ、実装はまだ）

- **STAGE1のハイブリッド案（ユーザー合意済み、実装未着手）:** 現状の広域検索に
  加えて「カテゴリ代表ペルソナ発の検索」を並走させ、STAGE4で両ルートの候補を
  同じ基準（変革ポテンシャル優先）で比較する案。
- **STAGE4のlife_relevance_score定義の明確化（提案のみ、ユーザーの明示的な
  合意はまだ得ていない）:** 「変革ポテンシャルが大きいが対象者が極端に少ない」
  ジレンマへの対処として、life_relevance_scoreを「対象者数の多さ」ではなく
  「主人公の具体的な状況の奥にある感情・欲求が、対象者でない視聴者にも
  当てはめられる普遍性」と再定義する案を提案した。CLAUDE.mdへの反映は
  まだしていない。

## 次にやること

上記3点（特に1・2）で合意が取れたら、`kl_paper_interest_score.py`を修正して
STAGE4を再実行する。CLAUDE.mdのSTAGE4節にも改訂の経緯として追記する
（過去の「査読済みのみルール撤回」等と同じ書き方で、撤回・改訂の理由を残す）。
